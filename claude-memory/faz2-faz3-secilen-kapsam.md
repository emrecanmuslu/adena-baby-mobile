---
name: faz2-faz3-secilen-kapsam
description: "Faz 1 sonrası seçilen kapsam (2026-06-11): yapılacak Faz 2/3 özellikleri + iptaller + öncelik"
metadata: 
  node_type: memory
  type: project
  originSessionId: b2cd8d6b-c272-4821-9101-d42e33bd35dd
---

2026-06-11'de FAZ 1 tamamlandıktan sonra kullanıcı sonraki kapsamı netleştirdi. Seçilenler **en öncelikli** ve bunlara başlanacak:

**YAPILACAK:**
- **Faz 2 → Anılar / "ilk"ler / fotoğraf günlüğü** ← İLK TESLİMAT YAPILDI (2026-06-11). Backend yeni `apps/memories` (Memory modeli: baby FK + date + title + note + photo ImageField + first_tag; SyncModel=UUID+audit+soft-delete; ListCreate+RetrieveUpdateDestroy generics, IsBabyMember; migration 0001; foto MEDIA multipart, offline-sync DIŞI). Mobil: `image_picker ^1.1.2`, `models/memory.dart` (+kFirstTags katalog emoji), `data/memory_repository.dart` (FormData multipart), `features/memories/memories_screen.dart` (aylara gruplu foto ızgara + ekle/detay sheet, kamera/galeri), router `/memories`, ayarlar menüsü "Anılar / Fotoğraf Günlüğü" (camera ikonu), iOS Info.plist NSCamera/NSPhotoLibrary izinleri. API_SOZLESME §10. **Django restart gerekir** (yeni app/migration). Milestone entegrasyonu sonraki adım.
- **Faz 2 → Milestone / kilometre taşı takibi** ← YAPILDI (2026-06-12). Backend `health` app'e eklendi: `milestone_catalog.py` (28 basamak, yaş ayı+kategori motor/social/language/cognitive, `ensure_milestones` idempotent lazy üretim, doğum tarihi GEREKMEZ), `Milestone` modeli (baby FK, key, category, title, expected_month, achieved, achieved_date; unique baby+key), `MilestoneSerializer`, `MilestonesView` GET + `MilestoneDetailView` PATCH, urls, migration 0003. Mobil: `models/milestone.dart` (+kategori meta+yaş etiketi), health_repository milestones()+setMilestoneAchieved()+milestonesProvider, `features/development/milestones_screen.dart` (yaşa gruplu liste + ilerleme kartı + "Şimdi" rozeti + başarıldı toggle), router `/milestones`, **Sağlık Hub'a** "Gelişim / Kilometre Taşları" köprüsü. API_SOZLESME §7. **Django restart gerekir**. Not: "ilk"ler (anılar first_tag) ile key'ler örtüşüyor (smile/word/...) — ileride otomatik eşleşme eklenebilir.
  - **Ana sayfa gösterimi (2026-06-12):** `home_screen.dart`'a `_MilestoneSection` eklendi — bebeğin yaşına uygun (expectedMonth ≤ yaş+2) henüz başarılmamış ilk 3 kilometre taşını "GELİŞİM" başlığıyla hatırlatır (unutulmasın); dokun→/milestones. Hepsi tamam/veri yoksa gizli. _UpcomingSection (aşı) altına yerleşti.

**FAZ 2 SEÇİLENLER TAMAM** (anılar/foto günlüğü + milestone + home gösterimi). Sıradaki: **FAZ 3** = (a) detaylı semptom/hastalık takibi, (b) diş çıkarma takibi, (c) uzman içeriği/ebeveyn topluluğu.
- **Faz 3 → Detaylı semptom/hastalık takibi** ← YAPILDI (2026-06-12). Yaklaşım: belirti = yeni **Record polimorfik tipi** `symptom` (timeline + offline-sync + ateş/ilaçla aynı sağlık hattı; ayrı model DEĞİL). Backend: `records/models.py` TYPES'a `("symptom","Belirti")` + migration `0002_alter_record_type` (max_length=12 yeterli, migrate edildi). Mobil: `RecordType.symptom`, yeni `models/symptom.dart` (kSymptoms 14 belirti emoji+key+tr katalog: cough/runny_nose/congestion/sneeze/vomit/diarrhea/constipation/rash/fussy/poor_appetite/sleep_trouble/ear_pain/eye_discharge/tummy_ache + SymptomSeverity mild/moderate/severe), `record_ui.dart` 5 switch + summary, `theme.dart` `symptom`(#D98AA6 gül/erik)+`symptomBg`, `adena_icons.dart` `pulse`(EKG) ikon, `record_form.dart` symptom case = `_SymptomGrid`(emoji pill Wrap seçim) + şiddet AdTabs + not (AdField info: rozetli), `health_screen.dart` "Son ateş & ilaç"→"Son sağlık kayıtları" belirti dahil + `_entryText` symptom. data={key,severity,note?}. Kayıt başına TEK belirti (çoklu için ayrı kayıt). analyze temiz, APK emulator-5554'e kuruldu + masaüstü AdenaBaby-test.apk güncellendi. Sıradaki: (b) diş çıkarma takibi.
- **Faz 3 → Diş çıkarma takibi** ← YAPILDI (2026-06-12). Yaklaşım: Milestone deseninin AYNISI (ayrı health modeli, idempotent katalog, doğum tarihi gerekmez). Backend `health` app: `teeth_catalog.py` (20 süt dişi = 2 çene×2 taraf×5 pozisyon; position 1=orta kesici…5=2.azı; TYPICAL_MONTH tipik çıkış ayı; `ensure_teeth` idempotent), `Tooth` modeli (baby FK, key, jaw upper/lower, side left/right, position, name TR, typical_month, erupted, erupted_date; unique baby+key), `ToothSerializer`, `TeethView` GET(ensure+list) + `ToothDetailView` PATCH(erupted+date, milestone gibi), urls `/babies/<id>/teeth` + `/teeth/<pk>`, migration `0004_tooth`. Mobil: `models/tooth.dart` (+positionLabel "Üst sağ"+typicalLabel), health_repository teeth()+setToothErupted()+teethProvider, `features/development/teeth_screen.dart` (ağız haritası: üst+alt çene kartları, her çene tek satır 10 diş sağ5→1·orta boşluk·sol1→5; çıkan diş dolu yeşil+tik, değilse pozisyon no'lu soluk; dokun→_ToothSheet tarih seç+çıktı/geri al; ilerleme X/20 yeşil kart; "Son çıkanlar" listesi), router `/teeth`, Sağlık Hub'a "Diş Gelişimi" köprüsü (tooth ikonu, pump/turkuaz). `adena_icons.dart` `tooth` ikon. **Django restart gerekir** (yeni views/urls). Kalan FAZ 3: (c) uzman içeriği/ebeveyn topluluğu.
  - **Diş ekranı GÖRSEL YENİLEME (2026-06-12):** İlk basit iki-sıra düzen kullanıcı isteğiyle GERÇEKÇİ AĞIZ HARİTASINA çevrildi. Kullanıcı tasarımı `design/AdenaBaby/Diş Gelişimi.html`'e ekledi (anatomik dental arch, üstten görünüm; tipe göre diş şekilleri SVG path SHAPES 1-5; parabolik kemer place() geometrisi; durum: çıktı yeşil+tik / sırada şeftali-kesikli / çıkmadı soluk; dil+diş eti+legend). `teeth_screen.dart` BU HTML'e birebir uyarlandı: diş şekilleri `flutter_svg` SvgPicture.string ile çiziliyor (tüm ağız tek SVG string = _mouthSvg, transform translate/rotate/scale + counter-rotate tik), tıklama GestureDetector + koordinat hit-test (en yakın diş <23px); çene başlıkları Flutter Text overlay; SVG feDropShadow/text kaldırıldı (flutter_svg uyumu). _ProgressHeader yeşil gradient+alt metin, _ToothSheet önizleme dişi+durum rozeti+Vazgeç. analyze temiz, APK emulator-5554 + masaüstü güncel. NOT: flutter_svg'nin nested transform/gradient render'ı cihazda görsel doğrulanmalı (sorun olursa CustomPainter'a geçilebilir).
- **Faz 3 → Uzman içeriği / ebeveyn topluluğu** (rehber içerik + forum/topluluk).

**ERTELENDİ (çok sonra):**
- **Faz 1.5 → AI uyku tahmini** (SweetSpot benzeri, TR). Yüksek Ar-Ge.

**İPTAL / kapsam dışı (geri açma):**
- Faz 2: haftalık-aylık recap & paylaşım, rakipten veri içe aktarma, premium hediye/davet, ikiz eş-zamanlı giriş, widget.
- Faz 3: Apple Watch / Wear OS.
- e-Nabız entegrasyonu (araştırma dahi yok), "Kapsam dışı" tablo (gebelik takibi/forum/kreş) — bakılmıyor.

Ayrıca açık: **monetizasyon/premium** Faz 1'de "her şey ücretsiz" → bir fazda yeniden tanımlanacak. bkz [[faz1-kararlari-2026-06-11]]
